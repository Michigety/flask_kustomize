apiVersion: v1
kind: Service
metadata:
  name: flask-health-check-service
  labels:
    app: flask-health-check
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: flask-health-check
